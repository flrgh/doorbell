#!/usr/bin/env bash

export _DOORBELL_TEST=1

exec -a busted \
    resty \
    -I "$PWD/lib" \
    -I "$PWD" \
    --no-stream \
    --http-include "./assets/nginx.include/http.shm.conf" \
    ./spec/busted "$@"
